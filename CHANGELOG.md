# 更新日志

本文件记录项目的重要变化。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [0.5.0]

相对 0.4.0 beta 本地构建的变化如下。0.5.0 的可公开分发 DMG 仍须完成 Developer ID 签名、Apple 公证与安装验收；代码版本或本地测试包不等于通过这些验证。

### 新增

- Apple Music 进度条支持实时拖动跳转；拖动期间更新岛上时间，并在提交前核对曲目，避免把进度写到另一首歌。
- 文件托盘支持锁定展开状态、单独移除文件临时副本；清空托盘后锁定状态仍可保持。
- 展开岛体新增电源按钮，可直接退出应用。
- 增加完整尺寸的应用图标资源，并在 README 展示图标。

### 修复

- 修复播放一段时间后点击“上一曲”只会从头重播当前歌曲的问题。
- 修复秒表、番茄钟和倒计时切换时，岛体与遮罩未随内容改变高度、导致数字被裁切的问题。
- 修复拖动音乐进度时，轮询结果可能覆盖实时预览；切歌、移开指针或切换界面时会结束或取消待处理的拖动。

### 优化

- 计时入口由扳手改为闹钟图标；音乐、文件托盘和三种计时模式按内容使用不同的展开高度。
- 岛体伸缩与计时内容淡入淡出分阶段衔接，并加快展开内容显现速度。
- 音乐进度条悬停时淡入白色拖动圆点，移开后淡出；支持无障碍进度调整。
- 文件增减、托盘清空、锁定和退出加入更清晰的动画与反馈；系统开启“减少动态效果”时，岛内退出不等待动画。
- Apple Music 自动化权限说明涵盖播放进度控制，并增加相关单元与渲染回归测试。

## [0.4.0-beta.1] - 2026-09-18

GitHub 上的 `local-2026-09-17-dmg` 预发布是 **未公证的本地构建**，不是 Developer ID 正式安装包；以下记录其代码基线。

### 新增

- 菜单栏常驻应用和贴合 Mac 刘海顶部的浮动 `NSPanel`。
- 系统音量读取、滑杆调节、增减和静音控制。
- Apple Music 曲目信息、封面、实时进度和播放控制。
- 文件临时托盘、拖放导入和系统 AirDrop 分享。
- 秒表、多个番茄钟时段和支持长按快速调节的倒计时。
- 音乐与计时活动的紧凑状态展示。
- 多显示器选择、拓扑变化监听和模拟屏幕调试器。
- 64 状态屏幕快照矩阵和关键系统场景稳定性测试。
- README、贡献说明、Issue/PR 模板和 GitHub Actions 持续集成。
- Developer ID Archive、公证、DMG、SHA-256、manifest、Tag 和 GitHub Release 自动化流程；此流程在本地预发布中未完成签名、公证与正式分发验证。
- 菜单栏脱敏调试报告、Beta 反馈入口和诊断信息单元测试。
- 可由 CI 校验的 9 类真机 Beta 矩阵、验收手册和 `S0`–`S3` 问题分级流程；真机矩阵仍待逐项验收。

### 调整

- 紧凑面板高度改为屏幕顶边与普通窗口可用顶边之间的保留区域高度。
- 面板展示状态切换改由 SwiftUI 驱动动画，避免与 AppKit frame 动画冲突。
- 音乐、文件托盘和计时工具之间加入一致的缓入缓出转场。
- 音乐播放与计时运行时使用更宽的紧凑岛展示实时状态。
- 项目、App、target、scheme、源码目录和测试目录统一改为英文 Dynamic Island 命名。
- Release 构建启用 Hardened Runtime，并禁止注入 `get-task-allow` 调试 entitlement。

### 修复

- 修复悬停延迟期间过早展开、离开后延迟任务未取消的问题。
- 修复音量、错误和指针反馈等普通状态触发多余面板 frame 更新的问题。
- 修复 Spaces、全屏、隐藏菜单栏、合盖切屏和 Apple Music 权限拒绝场景下的稳定性问题。
